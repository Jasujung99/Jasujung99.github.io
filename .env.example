# Copy this file to ".env" (local development only) and fill in the values.
# Do NOT commit your real secrets. This repository's .gitignore ignores .env files.

# Frontend (Vite) — optional override for subscription endpoint
VITE_SUBSCRIPTION_ENDPOINT=

# Mailchimp (used by GitHub Actions sync script)
MAILCHIMP_API_KEY=
MAILCHIMP_DC=
MAILCHIMP_LIST_ID=

# Notion (used by GitHub Actions sync script)
NOTION_TOKEN=
NOTION_DATABASE_ID=

# Copy this file to ".env" for local development if needed.
# Do NOT commit your real secrets. `.env` is ignored by .gitignore.

# Notion (used by your serverless backend such as Cloudflare Workers)
# These values are NOT read by the frontend. Configure them in your Worker dashboard.
NOTION_TOKEN=YOUR_NOTION_INTEGRATION_TOKEN
NOTION_DATABASE_ID=YOUR_NOTION_DATABASE_ID
